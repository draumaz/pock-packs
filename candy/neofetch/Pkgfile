name=neofetch
version=7.1.0
source=(https://github.com/dylanaraps/${name}/archive/refs/tags/${version}.tar.gz)

build() {

  install -dm755 "${PKG}"/"${POCK_SYS_PFX}"/bin
  mv ${name}-${version}/${name} "${PKG}"/"${POCK_SYS_PFX}"/bin

}
